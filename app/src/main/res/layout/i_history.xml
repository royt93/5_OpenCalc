<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".opencal.ui.home.HomeFragment">

        <TextView
            android:id="@+id/historyItemCalculation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/history_background_color"
            android:gravity="end"
            android:paddingHorizontal="10dp"
            android:paddingVertical="2dp"
            android:textColor="?attr/text_color"
            android:textSize="35sp"
            app:layout_constraintTop_toBottomOf="@+id/historyTime"
            tools:ignore="MissingConstraints" />

        <!-- N-DATA-4: ghim entry để không bị trim theo historySize / xoá bởi "Clear History" -->
        <ImageView
            android:id="@+id/ivHistoryPin"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="6dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_pin_history"
            android:padding="2dp"
            android:src="@drawable/ic_baseline_favorite_border_24"
            app:layout_constraintBottom_toBottomOf="@+id/historyItemResult"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/historyItemCalculation"
            app:tint="?attr/text_second_color" />

        <TextView
            android:id="@+id/historyItemResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/history_background_color"
            android:gravity="end"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:textColor="?attr/text_second_color"
            android:textSize="20sp"
            app:layout_constraintTop_toBottomOf="@+id/historyItemCalculation"
            tools:ignore="MissingConstraints" />

        <TextView
            android:id="@+id/historyTime"
            style="@style/RobotoFontCondensedMedium"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/history_background_color"
            android:gravity="start"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            android:textColor="?attr/text_second_color"
            android:textSize="22sp"
            tools:ignore="MissingConstraints" />

        <!-- Add more space when it's the same date than the next history element -->
        <View
            android:id="@+id/historySameDateSeparator"
            android:layout_width="match_parent"
            android:layout_height="18dp"
            android:background="?attr/history_background_color"
            app:layout_constraintTop_toBottomOf="@+id/historyItemResult"
            tools:ignore="MissingConstraints" />

        <View
            android:id="@+id/historySeparator"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="?attr/foreground_color"
            app:layout_constraintTop_toBottomOf="@+id/historyItemResult"
            tools:ignore="MissingConstraints" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
